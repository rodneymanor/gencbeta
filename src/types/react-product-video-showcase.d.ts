declare module "react-product-video-showcase" {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  export const ProductGrid: any;
  // fallback default (some builds may still export default)
  const RPVSDefault: any;
  export default RPVSDefault;
}

// fallback if direct path needed

// / <reference types="react" />

declare module "react-product-video-showcase/styles.css";
